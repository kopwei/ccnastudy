! Base configuration for Cisco 2960S
! This config restores basic management settings only.
! It does NOT modify usernames, passwords, or SSH keys.
! Note: IP address must be adjusted per device (see inventory.py)
hostname Switch2960S
!
line vty 0 4
 transport input ssh
 login local
!
end
